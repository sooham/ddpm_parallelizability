[project]
name = "ddpm-parallelizability"
version = "0.1.0"
description = "DDPM/DDIM experiments on MNIST and CIFAR-10"
requires-python = ">=3.10"
dependencies = [
    "datasets",
    "matplotlib",
    "torch",
    "torchvision",
    "wandb",
]

[dependency-groups]
dev = []
